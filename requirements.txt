# Browser Translator — Dependencies
# Install with: uv pip install -r requirements.txt
# Or from the repo: uv tool install -e .
#
# Core (required for speech translation):
uvicorn>=0.27
fastapi>=0.100
pydantic>=2.0
moonshine-voice>=0.0.62
piper-tts>=1.2.0
pydub>=0.25
aiohttp>=3.9

# OCR (optional — uncomment if you need page translation):
# paddleocr>=3.7
# paddlepaddle>=3.0
